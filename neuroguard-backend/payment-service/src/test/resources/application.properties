spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Disable Eureka and service discovery during tests
eureka.client.enabled=false
eureka.instance.enabled=false
spring.cloud.discovery.enabled=false
spring.cloud.netflix.enabled=false

# Disable Feign/remote calls by not scanning or use test configs
feign.hystrix.enabled=false

# Prevent attempts to reach external services
spring.main.allow-bean-definition-overriding=true
